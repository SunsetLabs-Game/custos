{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build && pnpm --filter @custos/web build",
  "outputDirectory": "apps/web/dist",
  "github": { "silent": true }
}
