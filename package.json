{
  "name": "custos",
  "private": true,
  "version": "0.1.0",
  "description": "On-device anti-scam shield for USDT sends — QVAC + TranslatePsy + WDK + Pears",
  "license": "MIT",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r --filter=./packages/** build",
    "test": "pnpm -r --filter=./packages/** --filter=./apps/** test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev:web": "pnpm --filter @custos/web dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
